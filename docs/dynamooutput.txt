(Ben notes):
This is a 1KB item from DynamoDB. If it gets any bigger, it will consume 2 WRUs when modified.

258 bytes for keys 'cmpnt', 'gamId', 'name', 'maxSz', 'isOpn' and their vals

230 bytes for key 'text' and the text block after it

8 bytes for the key 'sesns' (5 bytes) and overhead from pointing to a map (3 bytes)

112 bytes for the 'sesns' map keys (8 keys * (10 chars + 1 overhead per key + 3 from themselves being maps))

56 bytes for the 'sesns' submap keys (8 keys *(5 characters + 2*(1 overhead per submap key)))

360 bytes for the 'sesns' submap vals (8 keys *(36 + 9 characters))

= 1024 bytes


(Command, after manually setting up the table and item):
aws dynamodb update-item \
  --table-name "testgames" \
  --key '{
    "gamId": {"S": "grouping_flankers_swindler"},
    "cmpnt": {"S": "LOBBY"}
  }' \
  --update-expression "SET maxSz = :val" \
  --expression-attribute-values '{
    ":val": {"N": "6"}
  }' \
  --return-consumed-capacity TOTAL --return-values ALL_NEW \
  --profile {awsProfileName}

(Result):
Attributes:
  cmpnt:
    S: LOBBY
  gamId:
    S: grouping_flankers_swindler
  isOpn:
    BOOL: true
  maxSz:
    N: '6'
  gname:
    S: '50cha game name, repeat 4x to account for unicode.50cha game name, repeat
      4x to account for unicode.50cha game name, repeat 4x to account for unicode.50cha
      game name, repeat 4x to account for unicode.

      '
  sesns:
    M:
      blablablab:
        M:
          nam:
            S: BlanketedBlanketedBlanketedBlanketed
          ws:
            S: GGGG1234=
      cracracrac:
        M:
          nam:
            S: CrankiestCrankiestCrankiestCrankiest
          ws:
            S: FFFF1234=
      crecrecrec:
        M:
          nam:
            S: CrescendoCrescendoCrescendoCrescendo
          ws:
            S: IIII1234=
      dogdogdogd:
        M:
          nam:
            S: DogwalkerDogwalkerDogwalkerDogwalker
          ws:
            S: EEEE1234=
      eateateate:
        M:
          nam:
            S: EatsbreadEatsbreadEatsbreadEatsbread
          ws:
            S: CCCC1234=
      flyflyflyf:
        M:
          nam:
            S: FlyingbatFlyingbatFlyingbatFlyingbat
          ws:
            S: DDDD1234=
      krakrakrak:
        M:
          nam:
            S: KraftwerkKraftwerkKraftwerkKraftwerk
          ws:
            S: BBBB1234=
      strstrstrs:
        M:
          nam:
            S: StrongestStrongestStrongestStrongest
          ws:
            S: HHHH1234=
  text:
    S: An extremely long block of text that will cause me to consume more WRUs.An
      extremely long block of text that will cause me to consume more WRUs.An extremely
      long block of text that will cause me to consume more WRUs. 67 67 ^
ConsumedCapacity:
  CapacityUnits: 1.0
  TableName: testgames

(more notes): So I have 230 bytes...I want to encode 'ready', 'confirm ready' for each item in "sessions" (sesns).
I also want to encode the card game name and a reference to the rules params.

To actually start the game, all players' "ready" ('r') and "confirm ready" ('rr') hashes must match. The client should compute this hash
by taking the game's (rul + tcg + maxSz) values and hashing them, then taking the first 4 characters.

Foreach session key in sesn: add two subkeys, 'r'->'12345' and 'rr'->'12345' (8*(1+5+1) + 8*(2+5+1) = 76 bytes)

tcg: POKE or MAGI or YUGI (3+4 = 7 bytes)

rul: aa/relative/path/from/bucket/to/rule/metadata.json (3+50 = 53 bytes)

ttl: 1234567890 (3+10 = 13 bytes)

That's 149 characters, so I have 81 remaining.

Do we need a passphrase? "pswd": "######" would be 10B

NEED TO CHECK THAT WS CONNECTIONS CAP OUT AT 8 CHARS + the EQUALS SIGN (=)
